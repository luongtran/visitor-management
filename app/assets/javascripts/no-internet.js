function isOnline() {
    var online = navigator.onLine;    // Detecting the internet connection
    if(online) {
    	alert('You\'re Offline now...');
    } else {
       alert('You\'re Offline now...');
    }
}