<h3>Search visitors</h3>

<%= form_tag visitor_search_path, :remote => ajax do %>
	<%= text_field_tag 'search_key', '',:class => 'left-input' %>
	<%= submit_tag 'Search', :class => 'btn right-input' %>
<% end %>